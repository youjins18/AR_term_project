# chr_commander

Reserved keyboard input boundary. It intentionally publishes no command until the
key map and safety behavior are specified. Keeping the process separate prevents
terminal input handling from entering either controller.
